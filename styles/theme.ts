import { DefaultTheme } from "styled-components";

export const COLORS = {
  primary: "#13b38b",
  secondary: "#fff",
  tertiary: "#9457ff",
};

export const DEFAULT_THEME: DefaultTheme = {
  colors: COLORS,
};
